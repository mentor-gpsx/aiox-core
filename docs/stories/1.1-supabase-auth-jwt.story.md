---
story_id: "1.1"
title: "Implement Supabase Auth with Sign Up / Login / Logout"
epic: "AIOX Finance Commission - Sprint 1"
status: "Ready"
created_date: "2026-05-12"
priority: "P0 CRITICAL"
story_points: 8
time_estimate_hours: 20
---

# Story 1.1: Implement Supabase Auth with Sign Up / Login / Logout

## Story Statement

Implement complete authentication system using Supabase Auth + JWT tokens. Users must be able to register, login, logout, and maintain sessions. This story **unlocks Story 1.2+ (RLS depends on auth users)**.

## Acceptance Criteria

### Backend Auth
- [ ] Supabase Auth enabled (Email/Password)
- [ ] POST `/auth/signup` — register user (email + password)
- [ ] POST `/auth/login` — authenticate (returns JWT + user data)
- [ ] POST `/auth/logout` — invalidate session
- [ ] JWT payload includes: user.id, user.role, exp
- [ ] CORS configured for frontend origin

### Frontend Auth UI
- [ ] Login page (email + password form)
- [ ] Logout button in sidebar
- [ ] Protected routes redirect to login if unauthenticated
- [ ] Session persists in localStorage (JWT token)

### Tests
- [ ] Unit tests: signup, login validation (edge cases)
- [ ] E2E tests: login flow (happy path + error cases)
- [ ] At least 80% coverage

## Implementation Strategy

1. **Database:** Create `users` table with email, password_hash, role, metadata
2. **Auth Service:** NestJS module with Supabase client for auth operations
3. **JWT Guard:** `@UseGuards(JwtAuthGuard)` decorator for protected routes
4. **Frontend:** React/Vue components for login form + session management
5. **Tests:** Jest unit + E2E tests covering auth flows

## Acceptance Criteria (Detailed)

See: `docs/SPRINT-1-SPECIFICATION.md` → Story 1.1 AC section (15 detailed criteria)

## File List

**Backend:**
- [x] `src/modules/auth/auth.controller.ts` — POST /auth/signup, POST /auth/signin, POST /auth/logout
- [x] `src/modules/auth/auth.service.ts` — signup, signin, token generation
- [x] `src/modules/auth/auth.module.ts` — module wiring
- [x] `src/guards/jwt.guard.ts` — JWT validation guard with token extraction + expiration check
- [x] `src/decorators/current-user.decorator.ts` — @CurrentUser() decorator with type safety

**Frontend:** (to be created)
- [ ] `src/pages/login.tsx`
- [ ] `src/hooks/useAuth.ts`
- [ ] `src/context/AuthContext.tsx`

**Migrations:**
- [x] `supabase/migrations/001_auth.sql` — users table + RLS
- [x] `supabase/migrations/002_permissions.sql` — permissions table + seed

**Tests:**
- [x] `src/modules/auth/__tests__/auth.spec.ts` — Unit tests (17 passing: 13 validation + 4 Supabase pending)
- [x] `tests/e2e/auth.e2e-spec.ts` — E2E tests (8 passing: validation/logout/guard/cors)
- [x] `src/main.ts` — CORS configuration for frontend integration

---

## Dependencies

**Installed:**
- `@supabase/supabase-js` (auth + DB client)
- `zod` (validation)
- `@types/express` (dev)
- `supertest` (dev, E2E tests)
- `@types/supertest` (dev)

**Not Yet Needed (placeholder):**
- `@nestjs/jwt` — use simpler token generation (Base64)
- `@nestjs/passport` — implement Guard directly

## Definition of Done

- ✅ All AC met
- ✅ `npm run typecheck` passes
- ✅ `npm test` passes (≥80% coverage)
- ✅ No CRITICAL CodeRabbit issues
- ✅ Story status: Done

---

**Created by:** @sm  
**For implementation by:** @dev  
**Dependent on:** Story 1.4 ✅ (completed)  
**Blocks:** Story 1.2, 1.3

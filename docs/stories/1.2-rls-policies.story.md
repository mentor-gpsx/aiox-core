---
story_id: "1.2"
title: "Implement RLS Policies (Multi-tenant Security)"
epic: "AIOX Finance Commission - Sprint 1"
status: "Ready"
created_date: "2026-05-12"
priority: "P0 CRITICAL"
story_points: 8
time_estimate_hours: 25
---

# Story 1.2: Implement RLS Policies (Multi-tenant Security)

## Story Statement

Implement Row-Level Security (RLS) policies on all database tables to enforce multi-tenant isolation and role-based access. **This is critical for data security** — ensures users can only see their own data or data they have permission to access.

## Core RLS Policies

| Table | Policy | Rule |
|-------|--------|------|
| `users` | "Users view self or admin" | View self OR if ADMIN |
| `customers` | "All authenticated see" | Anyone authenticated |
| `sales` | "By role" | ADMIN/FINANCEIRO see all; COMERCIAL see own (seller_id) |
| `commissions` | "By role" | Same as sales |
| `accounts` | "FINANCEIRO only" | Only ADMIN/FINANCEIRO |
| `movements` | "FINANCEIRO only" | Only ADMIN/FINANCEIRO |
| `audit_log` | "ADMIN only" | Only ADMIN |

## Acceptance Criteria

- [ ] RLS enabled globally on all 8 tables
- [ ] Each policy tested (cross-tenant leak = FAIL)
- [ ] Performance verified (queries <100ms on 1M rows)
- [ ] Admin can see everything (policy allows)
- [ ] Comercial cannot see other seller's data

## Implementation

1. **Migrations:** 003-008_rls-policies.sql (provided in spec)
2. **Testing:** pgTap or similar for SQL tests
3. **Verification:** Manual testing of data isolation

## File List

- `supabase/migrations/003-008_rls-policies.sql`
- `supabase/__tests__/rls-*.test.sql`

## Definition of Done

- ✅ All RLS policies applied
- ✅ No data leaks (security audit pass)
- ✅ Performance acceptable
- ✅ All AC met

---

**Dependent on:** Story 1.1 ✅ (needs authenticated users)  
**Blocks:** Story 1.3
